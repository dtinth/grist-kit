# src/help/topics.ts is a documentation content registry: every topic entry is
# structurally identical ({ id, title, summary, body }), which trips copy-paste
# detection on anonymized string literals. docs/ is generated from it.
sonar.cpd.exclusions=src/help/topics.ts,docs/**
